{
  "_comment": "Hosts available to ssh_exec, postgres_query, pm2_status. 'key' references an entry in the 'keys' section below.",
  "hosts": {
    "production": {
      "ip": "1.2.3.4",
      "user": "ubuntu",
      "key": "main",
      "description": "Main production server"
    },
    "staging": {
      "ip": "5.6.7.8",
      "user": "ubuntu",
      "key": "main",
      "description": "Staging environment"
    }
  },
  "_comment_keys": "Mapping of key names to .pem file paths. Forward slashes work cross-platform. Tilde (~) is expanded to $HOME or %USERPROFILE%.",
  "keys": {
    "main": "/path/to/your-key.pem"
  }
}
